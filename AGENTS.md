# Agent Instructions

## Git Workflow
- Anytime you make or finish changes, always stage and commit the changes to git with a clear, descriptive conventional commit message.
- NEVER commit the `.agents/` folder, `.codex/`, or any configuration/credential files to Git. Keep them strictly in `.gitignore`.


""